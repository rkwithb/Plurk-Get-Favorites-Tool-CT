# build.yml
# Triggered by:
#   - Push of a version tag (v*.*.*) — full build + GitHub Release
#   - Manual workflow dispatch     — full build only, release step is skipped
#
# Jobs:
#   lint    — flake8 + import check (ubuntu-latest, fast gate)
#   build   — PyInstaller matrix across Linux / Windows / macOS (depends on lint)
#   release — creates GitHub Release and attaches binaries + static assets (tag push only)

name: Build & Release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:

jobs:

  # ── Lint & Import Check ────────────────────────────────────────────────────
  # Repeated intentionally from test.yml — build should never assume
  # test.yml already ran cleanly on the same commit.
  lint:
    name: Lint & Import Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install flake8

      - name: Run flake8
        run: |
          flake8 core/ ui/ main.py

      - name: Run import check
        run: |
          python -c "
          import core.logger
          import core.i18n
          import core.db
          import core.backup
          import core.export
          import core.server
          import ui.app
          print('All core modules imported successfully.')
          "

  # ── PyInstaller Matrix Build ───────────────────────────────────────────────
  build:
    name: Build — ${{ matrix.os }}
    needs: lint
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false   # let all platforms finish even if one fails
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: plurk-fav-linux
            pyinstaller_args: >-
              --onefile
              --windowed
              --name plurk-fav-linux
              ui/app.py

          - os: windows-latest
            artifact_name: plurk-fav-win.exe
            pyinstaller_args: >-
              --onefile
              --windowed
              --name plurk-fav-win
              ui/app.py

          - os: macos-latest
            artifact_name: plurk-fav-macos
            pyinstaller_args: >-
              --windowed
              --name "Plurk Get Favorites Tool"
              ui/app.py

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      # Linux — install tkinter system package (not bundled with Python on Ubuntu)
      - name: Install tkinter (Linux only)
        if: runner.os == 'Linux'
        run: sudo apt-get install -y python3-tk

      # Write core/version.py so the frozen binary can report its version.
      # Uses the git tag on tag-push triggers, or the branch name on workflow_dispatch.
      # This file is not committed to the repo — it is generated here at build time only.
      - name: Write version file
        shell: bash
        run: |
          echo "VERSION = \"${{ github.ref_name }}\"" > core/version.py

      - name: Build with PyInstaller
        run: pyinstaller ${{ matrix.pyinstaller_args }}

      # Verify the frozen binary starts cleanly and the --version flag works.
      # --version exits before any GUI or log file is created, so this is
      # a pure startup + import smoke test.
      - name: Smoke test (Linux)
        if: runner.os == 'Linux'
        run: |
          chmod +x dist/plurk-fav-linux
          dist/plurk-fav-linux --version

      - name: Smoke test (Windows)
        if: runner.os == 'Windows'
        run: dist/plurk-fav-win.exe --version

      - name: Smoke test (macOS)
        if: runner.os == 'macOS'
        run: |
          echo "--- dist/ contents ---"
          ls dist/
          binary="dist/Plurk Get Favorites Tool.app/Contents/MacOS/Plurk Get Favorites Tool"
          chmod +x "$binary"
          "$binary" --version

      # macOS — PyInstaller produces a .app bundle directory under dist/
      # Zip it so it can be uploaded as a single artifact file
      - name: Zip .app bundle (macOS only)
        if: runner.os == 'macOS'
        run: |
          echo "--- dist/ contents ---"
          ls dist/
          cd dist
          zip -r plurk-fav-macos.zip "Plurk Get Favorites Tool.app"

      # Upload built binary as a workflow artifact (available for all trigger types)
      - name: Upload artifact (Linux)
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@v4
        with:
          name: plurk-fav-linux
          path: dist/plurk-fav-linux

      - name: Upload artifact (Windows)
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: plurk-fav-win
          path: dist/plurk-fav-win.exe

      - name: Upload artifact (macOS)
        if: runner.os == 'macOS'
        uses: actions/upload-artifact@v4
        with:
          name: plurk-fav-macos
          path: dist/plurk-fav-macos.zip

  # ── GitHub Release ─────────────────────────────────────────────────────────
  # Runs only when triggered by a version tag push.
  # Skipped entirely on workflow_dispatch — use the artifacts from the build
  # job directly from the Actions run summary page instead.
  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')

    permissions:
      contents: write   # required to create releases and upload assets

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download Linux artifact
        uses: actions/download-artifact@v4
        with:
          name: plurk-fav-linux
          path: artifacts/

      - name: Download Windows artifact
        uses: actions/download-artifact@v4
        with:
          name: plurk-fav-win
          path: artifacts/

      - name: Download macOS artifact
        uses: actions/download-artifact@v4
        with:
          name: plurk-fav-macos
          path: artifacts/

      - name: Copy static assets to artifacts
        run: |
          cp index.html artifacts/
          cp style.css artifacts/
          cp -r locales artifacts/

      - name: Create GitHub Release and upload binaries + assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
          files: |
            artifacts/plurk-fav-linux
            artifacts/plurk-fav-win.exe
            artifacts/plurk-fav-macos.zip
            artifacts/index.html
            artifacts/style.css
            artifacts/locales/zh_TW.json
